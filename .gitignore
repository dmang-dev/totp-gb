# Build intermediates (everything except the published ROM artifacts)
*.gb
*.gbc
*.ihx
*.map
*.sym
*.noi
*.cdb
*.adb
*.lst
*.rst
*.asm
*.o
*.d
s.cdb

# ...but DO commit the published ROMs
!artifacts/*.gb
!artifacts/*.gbc

# GBDK toolchain (vendored locally, not committed)
gbdk/

# Reconstruction backup (used during git history setup)
.reconstruct_backup/

# SRAM saves
*.srm
*.sav

# OS / editor
Thumbs.db
.DS_Store
*.swp

# Throwaway scripts
totp_check.py
